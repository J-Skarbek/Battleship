const shipFactory = (length, name) => {
  const isSunk = false;
  const numberofHits = 0;
  const statusReport = (length) => {
    if (numberOfHits = length) {
      isSunk = true;
    }
  };
  return {
    length,
    name,
    isSunk,
  };
};
